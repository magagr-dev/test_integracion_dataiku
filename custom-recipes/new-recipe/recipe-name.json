{
    "meta" : {
        "label" : "My custom recipe",
        "icon" : "icon-puzzle-piece"
    },

    "kind" : "PYTHON",

    "selectableFromDataset": "main",

    "inputRoles" : [
        {
            "name": "inputDataset",
            "label": "Input dataset",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "outputRoles" : [
        {
            "name": "transformedDataset",
            "label": "Processed dataset",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],

    "params": [
        {   "name": "numTransform",
            "label":  "Number of transformations",
            "type":  "INT",
            "defaultValue": 1
        },
        {
            "name": "transform_list",
            "label": "Shape",
            "type": "SELECT",
            "selectChoices": [
                { "value": "none", "label": "Select a transformation" },
                { "value": "CCD", "label": "Drop correlated columns"},
                { "value": "LVCCD", "label": "Drop column with low variance"}
            ],
            "defaultValue": "none"
        }],
    "resourceKeys" : []
}
